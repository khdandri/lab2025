(Практичнa робота 5)

---

## Завдання 10

**Умова:** НВВ $X$ задана функцією щільності розподілу:
$$f(x) = \begin{cases} c \cdot \cos x, & -\frac{\pi}{2} \le x \le \frac{\pi}{2}, \\ 0, & |x| > \frac{\pi}{2}, \end{cases}$$
де $c$ – деяка константа. Знайти константу $c$, функцію розподілу $F(x)$, ймовірність події $|X| \le \frac{\pi}{4}$.

**Дано:**Функція щільності $f(x)$ визначена на $[-\frac{\pi}{2}, \frac{\pi}{2}]$.Необхідно знайти $c$, $F(x)$, $P(|X| \le \frac{\pi}{4})$.

**Розв'язання**

Знаходження константи $c$
Використовуємо умову нормування $\int_{-\infty}^{\infty} f(x) dx = 1$:
$$\int_{-\pi/2}^{\pi/2} c \cdot \cos x \, dx = 1$$
$$c \cdot \left[ \sin x \right]_{-\pi/2}^{\pi/2} = 1$$
$$c \cdot (1 - (-1)) = 1 \implies 2c = 1 \implies \mathbf{c = \frac{1}{2}}$$

2. Знаходження функції розподілу $F(x)$
$$F(x) = \int_{-\infty}^{x} f(t) dt$$
$x < -\frac{\pi}{2}$: $F(x) = 0$.
$-\frac{\pi}{2} \le x \le \frac{\pi}{2}$: $F(x) = \int_{-\pi/2}^{x} \frac{1}{2} \cos t \, dt = \frac{1}{2} \left[ \sin t \right]_{-\pi/2}^{x} = \mathbf{\frac{1}{2} (1 + \sin x)}$.
$x > \frac{\pi}{2}$: $F(x) = 1$.

3. Знаходження ймовірності $P(|X| \le \frac{\pi}{4})$
$$P(|X| \le \frac{\pi}{4}) = \int_{-\pi/4}^{\pi/4} \frac{1}{2} \cos x \, dx$$
$$P = \frac{1}{2} \left[ \sin x \right]_{-\pi/4}^{\pi/4} = \frac{1}{2} \left( \sin \left(\frac{\pi}{4}\right) - \sin \left(-\frac{\pi}{4}\right) \right)$$
$$P = \frac{1}{2} \left( \frac{\sqrt{2}}{2} - \left(-\frac{\sqrt{2}}{2}\right) \right) = \frac{1}{2} \cdot \sqrt{2} = \mathbf{\frac{\sqrt{2}}{2}}$$

**Відповідь:** 1.Константа: $c = \frac{1}{2}$. 2.Функція розподілу $F(x)$:$$F(x) = \begin{cases} 0, & x < -\frac{\pi}{2}, \\ \frac{1}{2} (1 + \sin x), & -\frac{\pi}{2} \le x \le \frac{\pi}{2}, \\ 1, & x > \frac{\pi}{2}. \end{cases}$$ 3.Ймовірність $P(|X| \le \frac{\pi}{4})$: $\frac{\sqrt{2}}{2}$.

---

## Завдання 11

**Умова:** Часовий інтервал між надходженнями пакетів даних у комп’ютерній
мережі зі швидкістю передачі даних 10 Мбіт/сек підприємства має
експоненціальний розподіл з параметром 𝜆 = 15 мкс. Знайти:
− середню довжину інтервалу;
− дисперсію довжини інтервалу;
− СКВ довжини інтервалу;
− ймовірність того, що часовий інтервал між надходженнями пакетів
перевищить 20мкс;
− ймовірність того, що часовий інтервал між надходженнями пакетів
буде у межах 10 < Х < 15 мкс.

**Дано:**$X \sim \text{Exp}(\lambda)$, $\lambda = 15 \text{ мкс}^{-1}$.

**Розв'язання**

1.$M(X)$: $1/\lambda = 1/15 \text{ мкс}$.
2.$D(X)$: $1/\lambda^2 = 1/225 \text{ мкс}^2$.
3.$\sigma(X)$: $1/\lambda = 1/15 \text{ мкс}$.
4.$P(X > 20)$: $e^{-\lambda \cdot 20} = e^{-15 \cdot 20} = e^{-300}$.
5.$P(10 < X < 15)$: $e^{-\lambda \cdot 10} - e^{-\lambda \cdot 15} = e^{-150} - e^{-225}$.

**Відповідь:** $M(X) = \mathbf{1/15}$.$D(X) = \mathbf{1/225}$.$\sigma(X) = \mathbf{1/15}$.$P(X > 20) = \mathbf{e^{-300}}$.$P(10 < X < 15) = \mathbf{e^{-150} - e^{-225}}$.

---

## Завдання 12

**Умова:** Параметри генератора псевдовипадкових чисел 𝑟𝑎𝑛𝑑𝑜𝑚, що входить
до інтегрованого середовища 𝑇𝑢𝑟𝑏𝑜 𝑃𝑎𝑠𝑐𝑎𝑙, за замовчанням дорівнюють 𝑎 = 0,
𝑏 = 1. Знайти:
− математичне сподівання;
− дисперсію;
− середнє квадратичне відхилення;
− ймовірність того, що 𝑋 > 0.5.

**Дано:**$X \sim U(a, b)$, $a = 0$, $b = 1$.

**Розв'язання**

1.$M(X)$: $\frac{a+b}{2} = \frac{0+1}{2} = \mathbf{0.5}$.
2.$D(X)$: $\frac{(b-a)^2}{12} = \frac{(1-0)^2}{12} = \mathbf{1/12}$.
3.$\sigma(X)$: $\sqrt{1/12} = \mathbf{\sqrt{3}/6}$.
4.$P(X > 0.5)$: $1 - F(0.5) = 1 - 0.5 = \mathbf{0.5}$.

**Відповідь:** $M(X) = \mathbf{0.5}$.$D(X) = \mathbf{1/12}$.$\sigma(X) = \mathbf{\sqrt{3}/6}$.$P(X > 0.5) = \mathbf{0.5}$.

---

## Завдання 13

**Умова:** НВВ $X$ має нормальний розподіл з параметрами $\mu$ та $\sigma$. Функція щільності нормального розподілу задана формулою:
$$f(x) = \frac{1}{\sigma\sqrt{2\pi}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}$$

**Дано:**$X \sim N(\mu, \sigma^2)$, $f(x)$ задана.

**Відповідь:** $F(x)$: $\int_{-\infty}^{x} f(t) dt = \mathbf{\Phi\left(\frac{x - \mu}{\sigma}\right)}$.$M(X)$: $\mathbf{\mu}$.$D(X)$: $\mathbf{\sigma^2}$.$P(a \le X \le b)$: $\mathbf{\Phi\left(\frac{b - \mu}{\sigma}\right) - \Phi\left(\frac{a - \mu}{\sigma}\right)}$.$P(|X - \mu| \le \delta)$: $2\Phi\left(\frac{\delta}{\sigma}\right)$.

---

## Завдання 14

**Умова:** Автомат виготовляє кульки. Кулька вважається стандартною, якщо
відхилення 𝑋 діаметра кульки від проектного розміру за абсолютною величиною
менша ніж 0.7 мм. Вважається, що НВВ 𝑋 має нормальний розподіл з СКВ 𝜎 =
0.4 мм. Знайти, скільки в середньому буде стандартних кульок серед 100
виготовлених.

**Дано:**$X \sim N(0, 0.4^2)$, $\mu = 0$, $\sigma = 0.4$. Стандартна: $|X| < 0.7$. $n = 100$.

**Розв'язання**

1.Ймовірність $P$ (стандартної кульки):
$$P(|X| < 0.7) = 2\Phi\left(\frac{0.7}{0.4}\right) = 2\Phi(1.75)$$
$\Phi(1.75) \approx 0.4599$ (за таблицею).
$$P \approx 2 \cdot 0.4599 = 0.9198$$

2.Середня кількість $M$:
$$M = n \cdot P = 100 \cdot 0.9198 = \mathbf{91.98}$$

**Відповідь:** $M approx 92$

---